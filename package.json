{
  "name": "prime-utils.js",
  "version": "1.0.0",
  "description": "js工具包 数据处理，校验处理，时间处理",
  "main": "js/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "webpack -p --config webpack.conf.js --mode development --progress --colors",
    "prod": "webpack -p --config webpack.conf.js --progress --colors",
    "publish":"npm publish dist"
  },
  "author": "liuk123",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {}
}
